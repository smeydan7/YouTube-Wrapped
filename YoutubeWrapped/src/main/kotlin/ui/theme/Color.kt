package com.example.youtubewrapped.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors
val YouTubeRed = Color(0xFFFF0000)  // YouTube's signature red
val DarkRed = Color(0xFFCC0000)     // Slightly darker red for buttons
val LightRed = Color(0xFFFF4D4D)

// Background Colors
val BlackBackground = Color(0xFF000000)  // Black background for dark UI sections
val WhiteBackground = Color(0xFFFFFFFF)  // White for contrast

// Text Colors
val TextWhite = Color(0xFFFFFFFF)  // White text
val TextBlack = Color(0xFF000000)  // Black text
val TextGray = Color(0xFF888888)   // Light gray for subtle text

// Secondary & Accent Colors
val LightGray = Color(0xFFD3D3D3)  // Used for input fields or secondary elements
val DarkGray = Color(0xFF444444)   // Dark gray for depth

// Borders & Highlights
val ButtonRed = Color(0xFFE60000)  // Slightly lighter red for buttons
val BorderGray = Color(0xFFAAAAAA) // Used for outlines and dividers